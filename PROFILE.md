# Latchkey Jobs API

Managed ephemeral GitHub Actions runners that repair failing builds mid-run. The Jobs API gives coding agents and CI systems direct access to a fresh Ubuntu 24.04 x86_64 runner that runs one command and is then destroyed, with a hosted MCP server, CLI, and llms.txt for agent-native access.

- **Provider:** https://latchkey.dev/
- **Documentation:** https://latchkey.dev/documentation
- **Base URL:** https://api.latchkey.dev
- **Tags:** CI/CD, DevOps, GitHub Actions, Ephemeral Compute, Build & Test Infrastructure, Agent-Native, AI Coding Agents
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 95/100

## APIs

- **Latchkey Jobs API** — REST API for creating, submitting, monitoring, and cancelling ephemeral runner jobs. Each job runs one command on a fresh Ubuntu 24.04 x86_64 machine that is destroyed afterwards. Uses HTTP Bearer auth with a Latchkey API key. (`https://api.latchkey.dev`)

> REST API host is https://api.latchkey.dev (per OpenAPI servers block); docs and machine-readable artifacts are served from latchkey.dev. Hosted MCP server confirmed at https://latchkey.dev/mcp (streamable-http, bearer auth) with a public manifest at https://latchkey.dev/.well-known/mcp/manifest.json (8 tools, 2 prompts). Additional agent files confirmed live: https://latchkey.dev/agent.txt, https://latchkey.dev/llms-full.txt, and .md page twins. A CLI exists (npm install -g @latchkeydev/cli) as an additional programmatic surface — locally installed, not cataloged as MCPServer. No AsyncAPI/GraphQL/Postman found; no published agent skill bundles. Caveat: the advertised catalog file at https://latchkey.dev/apis.json returns the site 404 page, and no ai-plugin manifest exists (resolves to 404). All other referenced artifacts resolve.

_Generated by apis.io from a community submission._